SELECT
    order_item_id,
    order_id,
    customer_id,
    product_id,
    order_date,
    channel,
    order_status,
    is_cancelled,
    is_resolved,
    quantity,
    unit_price,
    replacement_cost,
    discount_pct,
    gross_amount,
    discount_amount,
    net_sales,
    refund_amount,
    returned_units,
    final_net_sales,
    final_item_cogs,
    allocated_shipping_cost,
    net_profit
FROM {{ ref('int_order_items_enriched') }}